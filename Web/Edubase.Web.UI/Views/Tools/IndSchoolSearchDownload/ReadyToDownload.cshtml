@model Edubase.Services.Domain.ProgressDto

@{
    ViewBag.bodyClasses = "schools-search-page search-page";
    ViewBag.hideLogo = true;
}


<div class="breadcrumbs">
    <ol>
        <li>@Html.RouteLink("Home", "Homepage")</li>
        <li>@Html.RouteLink("Tools", "Tools")</li>
        <li>@Html.RouteLink("View independent schools' significant dates", "IndSchSearch")</li>
    </ol>
</div>

<div class="grid-row">
    <div class="column-full">
        <h1 class="heading-xlarge">Download data</h1>
    </div>
</div>
<div class="grid-row">
    <div class="column-two-thirds">
        <h2 class="bold-medium">@ViewBag.Subtitle</h2>
        <p class="font-small">Your requested file is ready to download.</p>

        <ul>
            <li class="download-list-item">
                <span class="icon-link-copy">
                    <a href="@Model.FileLocationUri" target="_blank" rel="nofollow">Data in XLSX format <span class="visually-hidden">Opens in new window</span></a>
                    (will download as a ZIP file: open the ZIP to access its content)
                </span>

            </li>
        </ul>
    </div>
    <div class="column-full">
        <div class="button-row">
            <br/>
            @Html.RouteLink("« Go back", "IndSchSearch", null, new {@class = "button button-secondary back-to-indi"})
        </div>
    </div>
</div>

@section ViewScripts
{
    <script>
        $('.back-to-indi').on('click', function(e) {
            e.preventDefault();
            window.history.go(-1);
        })
    </script>
}
